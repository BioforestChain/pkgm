{
  "name": "@bfchain/pkgm",
  "private": true,
  "packageManager": "yarn@1.22.0",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "i": "lerna bootstrap",
    "dev:script": "lerna run dev:script --stream --parallel",
    "build:script": "lerna run build:script --stream --parallel",
    "build:main": "lerna run build:main --stream --parallel",
    "build": "yarn build:script && yarn build:main",
    "dev:main": "lerna run dev:main,dev:script --stream --parallel",
    "tsc": "lerna run tsc --stream --parallel",
    "dev":"yarn run dev:main"
  }
}
